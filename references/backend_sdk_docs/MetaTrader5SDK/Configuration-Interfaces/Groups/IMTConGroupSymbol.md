[🏠 Document Start](../../README.md) / [Configuration Interfaces](../README.md) / [Groups](../Groups.md) / IMTConGroupSymbol

[Previous](IMTConGroup/SymbolGet.md) | [Next](IMTConGroupSymbol/Enumerations.md)

<a id="imtcongroupsymbol"></a>
# IMTConGroupSymbol (#imtcongroupsymbol)

The IMTConGroupSymbol class contains the following methods:

Method | Purpose  
---|---  
[Release](IMTConGroupSymbol/Release.md) | Delete the current object.  
[Assign](IMTConGroupSymbol/Assign.md) | Assign a passed object to the current one.  
[Clear](IMTConGroupSymbol/Clear.md) | Clear an object.  
[Default](IMTConGroupSymbol/Default.md) | Set default values for all parameters of group symbols.  
[Path](IMTConGroupSymbol/Path.md) | Get and set the path to a symbol or group of symbols that are subject to the special group settings.  
[TradeMode](IMTConGroupSymbol/TradeMode.md) | Get and set the symbol trading mode for the group.  
[TradeModeDefault](IMTConGroupSymbol/TradeModeDefault.md) | Get the default trading mode, which is set for the symbol.  
[ExecMode](IMTConGroupSymbol/ExecMode.md) | Get and set the symbol execution mode for the group.  
[ExecModeDefault](IMTConGroupSymbol/ExecModeDefault.md) | Get the default execution mode, which is set for the symbol.  
[FillFlags](IMTConGroupSymbol/FillFlags.md) | Get and set the types of filling allowed for a symbol in this group.  
[FillFlagsDefault](IMTConGroupSymbol/FillFlagsDefault.md) | Get default filling types set for a symbol.  
[ExpirFlags](IMTConGroupSymbol/ExpirFlags.md) | Get and set the types of order expiration allowed for a symbol in this group.  
[ExpirFlagsDefault](IMTConGroupSymbol/ExpirFlagsDefault.md) | Get default order expiration types set for a symbol.  
[SpreadDiff](IMTConGroupSymbol/SpreadDiff.md) | Get and set the difference between the symbol spread for the group and the default spread.  
[SpreadDiffDefault](IMTConGroupSymbol/SpreadDiffDefault.md) | Get the default spread difference set for a symbol.  
[SpreadDiffBalance](IMTConGroupSymbol/SpreadDiffBalance.md) | Get and set the balance of the spread difference for the group.  
[SpreadDiffBalanceDefault](IMTConGroupSymbol/SpreadDiffBalanceDefault.md) | Get the default balance of the spread difference set for a symbol.  
[StopsLevel](IMTConGroupSymbol/StopsLevel.md) | Get and set the price band, within which the group is not allowed to place stop orders for a symbol.  
[StopsLevelDefault](IMTConGroupSymbol/StopsLevelDefault.md) | Get the default band of stop orders set for a symbol.  
[FreezeLevel](IMTConGroupSymbol/FreezeLevel.md) | Get and set the price band, within which it is not allowed to modify orders and positions for the group.  
[FreezeLevelDefault](IMTConGroupSymbol/FreezeLevelDefault.md) | Get and set the price band from the current price, within which the group is not allowed to modify orders and positions for a symbol.  
[VolumeMin](IMTConGroupSymbol/VolumeMin.md) | Get and set the minimum volume of trade operations for a symbol for the group.  
[VolumeMinExt](IMTConGroupSymbol/VolumeMinExt.md) | Get and set the minimum volume (with extended accuracy) of trade operations for a symbol for the group.  
[VolumeMinDefault](IMTConGroupSymbol/VolumeMinDefault.md) | Get the default value of the minimum volume of trade operations for a symbol.  
[VolumeMinExtDefault](IMTConGroupSymbol/VolumeMinExtDefault.md) | Get the default value of the minimum volume (with extended accuracy) of trade operations for a symbol.  
[VolumeMax](IMTConGroupSymbol/VolumeMax.md) | Get and set the maximum volume of trade operations for a symbol for the group.  
[VolumeMaxExt](IMTConGroupSymbol/VolumeMaxExt.md) | Get and set the maximum volume (with extended accuracy) of trade operations for a symbol for the group.  
[VolumeMaxDefault](IMTConGroupSymbol/VolumeMaxDefault.md) | Get the default value of the maximum volume of trade operations for a symbol.  
[VolumeMaxExtDefault](IMTConGroupSymbol/VolumeMaxExtDefault.md) | Get the default maximum volume (with extended accuracy) of trade operations for a symbol.  
[VolumeStep](IMTConGroupSymbol/VolumeStep.md) | Get and set the step of change of trade operations volume for a symbol for the group.  
[VolumeStepExt](IMTConGroupSymbol/VolumeStepExt.md) | Get and set the step of change of trade operations volume (with extended accuracy) for a symbol for the group.  
[VolumeStepDefault](IMTConGroupSymbol/VolumeStepDefault.md) | Get the default value of the volume change step for trade operations for a symbol.  
[VolumeStepExtDefault](IMTConGroupSymbol/VolumeStepExtDefault.md) | Get the default value of the volume change step (with extended accuracy) for trade operations for a symbol.  
[VolumeLimit](IMTConGroupSymbol/VolumeLimit.md) | Get and set the maximum allowable aggregate volume of positions and orders for a symbol in one direction for this group.  
[VolumeLimitExt](IMTConGroupSymbol/VolumeLimitExt.md) | Get and set the maximum aggregate volume (with extended accuracy) of positions and orders for a symbol for this group.  
[VolumeLimitDefault](IMTConGroupSymbol/VolumeLimitDefault.md) | Get the default maximum allowable aggregate volume of positions and orders in one direction for a symbol.  
[VolumeLimitExtDefault](IMTConGroupSymbol/VolumeLimitExtDefault.md) | Get the default maximum aggregate volume of positions and orders for a symbol with extended accuracy.  
[MarginFlags](IMTConGroupSymbol/MarginFlags.md) | Get and set additional modes of symbol margin checking for the group.  
[MarginFlagsDefault](IMTConGroupSymbol/MarginFlagsDefault.md) | Get default additional margin checking modes set for a symbol.  
[MarginInitial](IMTConGroupSymbol/MarginInitial.md) | Get and set the size of initial symbol margin for the group.  
[MarginInitialDefault](IMTConGroupSymbol/MarginInitialDefault.md) | Get the default initial margin set for a symbol.  
[MarginMaintenance](IMTConGroupSymbol/MarginMaintenance.md) | Get and set the size of symbol maintenance margin for the group.  
[MarginMaintenanceDefault](IMTConGroupSymbol/MarginMaintenanceDefault.md) | Get the default maintenance margin set for a symbol.  
[MarginRateInitial](IMTConGroupSymbol/MarginRateInitial.md) | Get the initial margin rate for orders of the specified type for the given group.  
[MarginRateInitialDefault](IMTConGroupSymbol/MarginRateInitialDefault.md) | Get the initial margin rate for orders of the specified type set on default for the symbol.  
[MarginRateMaintenance](IMTConGroupSymbol/MarginRateMaintenance.md) | Get the maintenance margin rate for orders of the specified type for the given group.  
[MarginRateMaintenanceDefault](IMTConGroupSymbol/MarginRateMaintenanceDefault.md) | Get the maintenance margin rate for orders of the specified type set on default for the symbol.  
[MarginRateLiquidity](IMTConGroupSymbol/MarginRateLiquidity.md) | Get and set symbol liquidity rate for this group.  
[MarginRateLiquidityDefault](IMTConGroupSymbol/MarginRateLiquidityDefault.md) | Get the default liquidity rate of the symbol.  
[MarginRateCurrency](IMTConGroupSymbol/MarginRateCurrency.md) | Get and set the margin currency rate for this group.  
[MarginRateCurrencyDefault](IMTConGroupSymbol/MarginRateCurrencyDefault.md) | Get the default margin currency rate set for the symbol.  
[MarginLong](IMTConGroupSymbol/MarginLong.md) | Get and set the group margin ratio for long positions and orders for a symbol. The method is obsolete.  
[MarginLongDefault](IMTConGroupSymbol/MarginLongDefault.md) | Get the default margin ratio for long positions and orders set for a symbol. The method is obsolete.  
[MarginShort](IMTConGroupSymbol/MarginShort.md) | Get the group margin ratio for short positions and orders for a symbol. The method is obsolete.  
[MarginShortDefault](IMTConGroupSymbol/MarginShortDefault.md) | Get and set margin ratio for short positions and orders set for a symbol. The method is obsolete.  
[MarginLimit](IMTConGroupSymbol/MarginLimit.md) | Get and set the group margin ratio of limit orders for a symbol. The method is obsolete.  
[MarginLimitDefault](IMTConGroupSymbol/MarginLimitDefault.md) | Get the default margin ratio of limit orders set for a symbol. The method is obsolete.  
[MarginStop](IMTConGroupSymbol/MarginStop.md) | Get and set the group margin ratio of stop orders for a symbol. The method is obsolete.  
[MarginStopDefault](IMTConGroupSymbol/MarginStopDefault.md) | Get the default margin ratio for stop orders set for a symbol. The method is obsolete.  
[MarginStopLimit](IMTConGroupSymbol/MarginStopLimit.md) | Get and set the group margin ratio of stop-limit orders for a symbol. The method is obsolete.  
[MarginStopLimitDefault](IMTConGroupSymbol/MarginStopLimitDefault.md) | Get the default margin ratio for stop-limit orders set for a symbol. The method is obsolete.  
[MarginHedged](IMTConGroupSymbol/MarginHedged.md) | Get and set the value of the hedged margin.  
[MarginHedgedDefault](IMTConGroupSymbol/MarginHedgedDefault.md) | Get the default hedged margin set for a symbol.  
[SwapMode](IMTConGroupSymbol/SwapMode.md) | Get and set the swap calculation mode for a certain symbol for the group.  
[SwapModeDefault](IMTConGroupSymbol/SwapModeDefault.md) | Get the default swap calculation mode for a symbol.  
[SwapLong](IMTConGroupSymbol/SwapLong.md) | Get and set the long position swap for a symbol for the group.  
[SwapLongDefault](IMTConGroupSymbol/SwapLongDefault.md) | Get the default long position swap set for a symbol.  
[SwapShort](IMTConGroupSymbol/SwapShort.md) | Get and set the short position swap for a symbol for the group.  
[SwapShortDefault](IMTConGroupSymbol/SwapShortDefault.md) | Get the default short position swap set for a symbol.  
[Swap3Day](IMTConGroupSymbol/Swap3Day.md) | Get and set the day to charge triple swap for a symbol for this group. The method is obsolete.  
[Swap3DayDefault](IMTConGroupSymbol/Swap3DayDefault.md) | Get the default triple swap charging day set for a symbol. The method is obsolete.  
[SwapYearDays](IMTConGroupSymbol/SwapYearDays.md) | Get and set the number of days in a year used in calculating swap percent for a given group.  
[SwapYearDaysDefault](IMTConGroupSymbol/SwapYearDaysDefault.md) | Get the default number of days in a year specified for the symbol.  
[SwapFlags](IMTConGroupSymbol/SwapFlags.md) | Get and set additional symbol swap settings for the given group.  
[SwapFlagsDefault](IMTConGroupSymbol/SwapFlagsDefault.md) | Get additional default swap settings specified for the given symbol.  
[SwapRateSunday](IMTConGroupSymbol/SwapRateSunday.md) | Get and set Sunday swap multiplier in symbol settings for the given group.  
[SwapRateSundayDefault](IMTConGroupSymbol/SwapRateSundayDefault.md) | Get the default Sunday swap multiplier specified for the symbol.  
[SwapRateMonday](IMTConGroupSymbol/SwapRateMonday.md) | Get and set Monday swap multiplier in symbol settings for the given group.  
[SwapRateMondayDefault](IMTConGroupSymbol/SwapRateMondayDefault.md) | Get the default Monday swap multiplier specified for the symbol.  
[SwapRateTuesday](IMTConGroupSymbol/SwapRateTuesday.md) | Get and set Tuesday swap multiplier in symbol settings for the given group.  
[SwapRateTuesdayDefault](IMTConGroupSymbol/SwapRateTuesdayDefault.md) | Get the default Tuesday swap multiplier specified for the symbol.  
[SwapRateWednesday](IMTConGroupSymbol/SwapRateWednesday.md) | Get and set Wednesday swap multiplier in symbol settings for the given group.  
[SwapRateWednesdayDefault](IMTConGroupSymbol/SwapRateWednesdayDefault.md) | Get the default Wednesday swap multiplier specified for the symbol.  
[SwapRateThursday](IMTConGroupSymbol/SwapRateThursday.md) | Get and set Thursday swap multiplier in symbol settings for the given group.  
[SwapRateThursdayDefault](IMTConGroupSymbol/SwapRateThursdayDefault.md) | Get the default Thursday swap multiplier specified for the symbol.  
[SwapRateFriday](IMTConGroupSymbol/SwapRateFriday.md) | Get and set Friday swap multiplier in symbol settings for the given group.  
[SwapRateFridayDefault](IMTConGroupSymbol/SwapRateFridayDefault.md) | Get the default Friday swap multiplier specified for the symbol.  
[SwapRateSaturday](IMTConGroupSymbol/SwapRateSaturday.md) | Get and set Saturday swap multiplier in symbol settings for the given group.  
[SwapRateSaturdayDefault](IMTConGroupSymbol/SwapRateSaturdayDefault.md) | Get the default Saturday swap multiplier specified for the symbol.  
[RETimeout](IMTConGroupSymbol/RETimeout.md) | Get and set the time during which the price issued by a dealer in the request execution mode is valid.  
[RETimeoutDefault](IMTConGroupSymbol/RETimeoutDefault.md) | Get the default time during which the price issued by a dealer in the request execution mode is valid.  
[REFlags](IMTConGroupSymbol/REFlags.md) | Get request execution flags for this group.  
[REFlagsDefault](IMTConGroupSymbol/REFlagsDefault.md) | Get default request execution flags.  
[IECheckMode](IMTConGroupSymbol/IECheckMode.md) | Get and set the mode of checking during instant execution set for a group.  
[IECheckModeDefault](IMTConGroupSymbol/IECheckModeDefault.md) | Get the default mode of checking for instant execution set for a symbol.  
[IETimeout](IMTConGroupSymbol/IETimeout.md) | Get and set the maximum allowed difference between the time of arrival of the price, at which the client places an order, and the time of the last price.  
[IETimeoutDefault](IMTConGroupSymbol/IETimeoutDefault.md) | Get the default maximum allowed difference between the time of arrival of the price, at which the client places an order, and the time of the last price.  
[IESlipProfit](IMTConGroupSymbol/IESlipProfit.md) | Get and set the maximum allowed slippage in the profitable direction during instant execution.  
[IESlipProfitDefault](IMTConGroupSymbol/IESlipProfitDefault.md) | Get the default maximum allowed slippage in the profitable direction during instant execution.  
[IESlipLosing](IMTConGroupSymbol/IESlipLosing.md) | Get and set the maximum allowed slippage in the loss direction during instant execution.  
[IESlipLosingDefault](IMTConGroupSymbol/IESlipLosingDefault.md) | Get the default maximum allowed slippage in the loss direction during instant execution.  
[IEVolumeMax](IMTConGroupSymbol/IEVolumeMax.md) | Get and set the maximum volume of a trade operation that can be executed in the instant execution mode.  
[IEVolumeMaxExt](IMTConGroupSymbol/IEVolumeMaxExt.md) | Get and set the maximum volume (with extended accuracy) of a trade operation that can be executed in the instant execution mode.  
[IEVolumeMaxDefault](IMTConGroupSymbol/IEVolumeMaxDefault.md) | Get the default maximum volume of a trade operation that can be executed in the instant execution mode.  
[IEVolumeMaxExtDefault](IMTConGroupSymbol/IEVolumeMaxExtDefault.md) | Get the default maximum volume (with extended accuracy) of a trade operation that can be executed in the instant execution mode.  
[IEFlags](IMTConGroupSymbol/IEFlags.md) | Get and set flags for the instant execution mode ([IMTConSymbol::EXECUTION_INSTANT (#enexecutionmode)](../Symbols/IMTConSymbol/Enumerations.md#enexecutionmode)).  
[IEFlagsDefault](IMTConGroupSymbol/IEFlagsDefault.md) | Get the [default](../Symbols/IMTConSymbol/IEFlags.md) flags for the instant execution mode ([IMTConSymbol::EXECUTION_INSTANT (#enexecutionmode)](../Symbols/IMTConSymbol/Enumerations.md#enexecutionmode)).  
[OrderFlags](IMTConGroupSymbol/OrderFlags.md) | Get and set the flags of order types allowed for the symbol.  
[OrderFlagsDefault](IMTConGroupSymbol/OrderFlagsDefault.md) | Get the default allowed order types set for the symbol.  
[PermissionsFlags](IMTConGroupSymbol/PermissionsFlags.md) | Get and set permission flags for the group symbols.  
[BookDepthLimit](IMTConGroupSymbol/BookDepthLimit.md) | Get and set a limit on the number of orders in the Market Depth window displayed for this particular group.  
  
The IMTConGroupSymbol contains the following enumerations:

Enumeration | Purpose  
---|---  
[EnREFlags (#enreflags)](IMTConGroupSymbol/Enumerations.md#enreflags) | Request execution settings.  
[EnPermissionsFlags (#enpermissionsflags)](IMTConGroupSymbol/Enumerations.md#enpermissionsflags) | Flags of permissions by the group symbols.  
  
<a id="default"></a>
## Use of Default methods (#default)

Settings of trading instruments are defined by properties from [IMTConSymbol](../Symbols/IMTConSymbol.md). They include basic symbol settings used for all clients by default. The platform allows overriding symbol settings for each client group. This can be done using the IMTConGroupSymbol class. In addition to Get and Set method used for working with overridden settings, the class also features Default methods, such as [IMTConGroupSymbol::TradeModeDefault](IMTConGroupSymbol/TradeModeDefault.md). These methods can be used for the following purposes:

  * Checking if a certain symbol property has a default value for the group
  * Setting symbol properties to default for the group



> Default methods do not return explicits value of basic symbol parameters and do not set default values.

In order to check whether a group's symbol property was inherited from a basic parameter, compare its value with the value of the corresponding Default method. For example:
    
    
    //--- Creating an object of a client group's symbol settings
    IMTConGroupSymbol* group_symbol;
    ...
    //--- Checking if the default value is used
    if(group_symbol->TradeMode()==group_symbol->TradeModeDefault())
      {
       //--- A default value is set
      }
    else
      {
       //--- An overridden value is set
      }

The set a symbol property to a default value, assign to it the value returned by the corresponding Default method. For example:
    
    
    //--- Creating an object of a client group's symbol settings
    IMTConGroupSymbol* group_symbol;
    ...
    //--- Assigning a default value
    if(group_symbol->TradeMode(group_symbol->TradeModeDefault())!=MT_RET_OK)
      {
       //--- Setting a default value failed
      }

<a id="example"></a>
## Example of how to request a list of available symbols for a group (#example)

This Server API example enables the receiving of a list of symbols available for the client group. The list is printed to the Journal in the order of symbol configurations in the group settings.
    
    
    //+------------------------------------------------------------------+
    //| Get available symbols in a group by client login                 |
    //+------------------------------------------------------------------+
    MTAPIRES CPluginInstance::GetGroupSymbols(UINT login)
      {
        //--- Check
        if(!m_api)
          {
           m_api->LoggerOut(MTLogErr, L"Failed to get group symbols by user, api interface not found");
           return(MT_RET_ERROR);
          }
        //--- Create objects
        MTAPIRES retcode;
        IMTUser* user = m_api->UserCreate();
        IMTConGroup* group = m_api->GroupCreate();
        IMTConGroupSymbol* group_symbol = m_api->GroupSymbolCreate();
        IMTConSymbol* symbol = m_api->SymbolCreate();
        //--- get account information
        if(retcode=m_api->UserGet(login,user) != MT_RET_OK)
          {
           m_api->LoggerOut(MTLogErr, L"Failed to get user info");
           return(retcode);
          }
        //--- Get the group of the client
        if(retcode = m_api->GroupGet(user->Group(), group) != MT_RET_OK)
          {
           m_api->LoggerOut(MTLogErr, L"Failed to get user group");
           return(retcode);
          }
        //--- Get the list of symbols for the group
        for(int i = 0; i < group->SymbolTotal(); i++)
          {
            //--- Get entries one by one
            if(retcode = group->SymbolNext(i, group_symbol) !=MT_RET_OK)
              {
               m_api->LoggerOut(MTLogErr, L"Failed to get group symbol");
               return(retcode);
              }
            //--- If the entry is a subdirectory, show all symbol from it, otherwise display the path to the symbol
            if(CMTStr::Find(group_symbol->Path(), L"*") >= 0)          
              {
                for(int m = 0; m < m_api->SymbolTotal(); m++)
                  {
                   if(retcode = m_api->SymbolNext(m, symbol) != MT_RET_OK)
                     {
                      m_api->LoggerOut(MTLogOK, L"Failed to get symbol configuration");
                      return(retcode);
                     }
                   //--- remove '*' from the path
                   CMTStr128 trim_path = group_symbol->Path();
                   trim_path.Trim(trim_path.Len()-1);
                   //--- If path to the group symbol is the same as the beginning of the full symbol path, show the full path of the symbol
                   if(CMTStr::Find(symbol->Path(), trim_path.Str()) == 0)
                     {
                       m_api->LoggerOut(MTLogOK, L"Symbol %s", symbol->Path());
                       retcode = MT_RET_OK;
                     }
                  }
              }
            else
              {
               m_api->LoggerOut(MTLogOK, L"Symbol %s",group_symbol->Path());
               retcode = MT_RET_OK;
              }
          }
        //--- Release memory
        user->Release();
        group->Release();
        group_symbol->Release();
        symbol->Release();  
        //--- All done
        return(retcode);
      }
    //+------------------------------------------------------------------+
